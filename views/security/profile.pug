extends ../layouts/main-layout.pug

block content
    if !profileUser
        span.errorMessage Check the url you are trying access
    else
        .profileHeaderContainer
            .coverPhotoContainer
                .userImageContainer
                    img(src=profileUser.profilePict, alt="User profile image")
        .postsContainer
        +createPostModal(userLoggedIn)

block scripts
    //- script(src="/js/profile.js")